spring:
#  h2:
#      console:
#        enabled: true
#        path: /h2-console
#  datasource:
#      url: jdbc:h2:mem:testdb
#      username: sa
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/HappyManwon?createDatabaseIfNotExist=true&useSSL=true
    username: root
    password: root1234!!

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true
    properties:
      hibernate:
        show_sql: true
        format_sql: true

server:
  address: localhost
  port: 8080

kakao:
  rest_api_key : KakaoAK 2d10cd365cbf8f56eda4e243e68f505e
